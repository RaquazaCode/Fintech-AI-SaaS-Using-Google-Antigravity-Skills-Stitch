import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "TradeApp - Financial Advisor SaaS",
  description: "A modern trading platform dashboard with real-time market data, portfolio tracking, and AI-powered insights",
  keywords: ["trading", "stocks", "forex", "crypto", "portfolio", "finance", "investment"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="antialiased">
        {children}
      </body>
    </html>
  );
}
